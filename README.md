# Forge Studio — Tester Distribution

This repo hosts **private tester builds** of Forge Studio and the update feed.
It contains the built app and `version.json` only — **no source code**.

- Latest tester build: see the [Releases](../../releases) page.
- The app checks `version.json` on startup to notify testers of new builds.
